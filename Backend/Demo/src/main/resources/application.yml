jwt:
  secret: "secret"

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/DemoDB 
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true